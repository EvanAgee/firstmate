---
tags:
  - firstmate
  - instructions
date: 2026-09-21
issue: 5175
walked: d760bf35
---

## What I walked

I spawned a Codex scout in a throwaway `FM_HOME` against commit `d760bf35`.
It read `AGENTS.md` and `.agents/skills/relay-reference/SKILL.md` and returned these exact passages:

> 5. **Report outcomes faithfully.**
> If work failed, say so plainly with the evidence.

> A promised final public reply is durable state, never conversation memory.

The scout reported no project edits.
I also spawned a Kimi scout in the same throwaway home.
Its launcher reached a first-run migration screen, then opened Kimi Code 2.0.2 without a configured model and requested login.
The installed Kimi CLI also reported `LLM not set, send "/login" to login` in the isolated profile.
The Kimi quote check remains blocked on an authenticated Kimi model.

The size test failed at the original 85,264 bytes and passed at 32,186 bytes.
The CI workflow tests, document audience check, coverage check, and root lint passed.

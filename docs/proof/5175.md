---
tags:
  - firstmate
  - instructions
date: 2026-09-21
issue: 5175
walked: 4459921d
---

## What I walked

I spawned a Codex scout in a throwaway `FM_HOME` against commit `d760bf35`.
It read `AGENTS.md` and `.agents/skills/relay-reference/SKILL.md` and returned these exact passages:

> 5. **Report outcomes faithfully.**
> If work failed, say so plainly with the evidence.

> A promised final public reply is durable state, never conversation memory.

The scout reported no project edits.
I ran `pi -p --no-extensions --no-session --approve --tools read --model phala/moonshotai/kimi-k3` inside a separate scratch checkout at commit `56e665bb`, with a scratch `FM_HOME`.
This used the authenticated Kimi K3 model through pi, not the Kimi Code CLI.
The model quoted the same fifth hard rule from `AGENTS.md:41-42` and the same promised public reply sentence from `.agents/skills/relay-reference/SKILL.md:13`.
The pi run used only its read tool and reported no edits or commands.
After rebasing onto `kun/main`, I ran the same Kimi K3 check in a scratch checkout at commit `4459921d`.
It returned both exact passages again from `AGENTS.md:41-42` and `.agents/skills/relay-reference/SKILL.md:13`.

The size test failed at the original 85,264 bytes and passed at 32,186 bytes before the rebase and 32,405 bytes after it.
The CI workflow tests, document audience check, coverage check, and root lint passed.

---
tags: [instructions, harness]
date: 2026-09-23
issue: 5175
walked: bf6a5175
---

# AGENTS.md under the harness instruction cap

## Cap and sizes

The cap comes from the parent repository's #5175 fix, whose size test fails at 32,768 bytes, the Codex project-instruction limit.
Before this change the fork's `AGENTS.md` was 72,406 bytes, so a harness that enforces that cap loaded a truncated contract.
After this change it is 32,364 bytes, 404 bytes under the cap.

`bash tests/fm-agents-size.test.sh` against the original file printed `not ok - AGENTS.md is 72406 bytes; harness instruction cap is 32768 bytes`.
Against the new file it printed `ok - AGENTS.md fits the 32768-byte harness instruction cap`.

## Coverage

`tests/fm-agents-coverage.test.sh` checks that every sentence of the pre-move `AGENTS.md`, frozen in `tests/fixtures/agents-md-before-5175.snapshot`, appears word for word in `AGENTS.md` or some `.agents/skills/*/SKILL.md`.
It also checks that every agent-only internal skill is named by a load trigger in `AGENTS.md`.
The one deliberate rewording, the red-merge rule's cross-reference to the captain instruction precedence section, is removed from the snapshot as the test header describes.

On the first cut of this change, the sentence check reported 113 missing sentences, which is how the review's dropped rules would have been caught.
On control copies of the final tree, the test failed and named the gap after each of three removals: the "Complexity alone is not expansion" sentence, the `firstmate-orca` trigger, and the moved `bin/fm-flow.sh` sentence in `project-management`.
The unchanged copy passed.

## What I walked

I cloned branch `fm/fm-5175-fork-land` at `bf6a5175` into a scratch directory as a plain checkout, pointed its `origin` at an unreachable URL, and created an empty `state/` directory.
The session-open hook runs only when `state/` exists (`bin/fm-primary-scope-lib.sh`), which is also why it stays silent in a linked task worktree.

I started a fresh `claude -p` session (Claude Code 2.1.280, `claude-opus-5-5`) in that clone with `FM_API=0`, and with shell and edit tools disabled.
Its session-open hook ran the digest, which reached the session in full: SESSION START with LOCK, BOOTSTRAP, and WAKE QUEUE, then SUPERVISION OPERATING INSTRUCTIONS for claude, READ-ONCE CONTRACT, FLEET STATE, NETWORK CHECKS, CONTEXT, and NEXT STEP.
Its LOCK line read `lock acquired: harness pid 72851`, and the clone's `state/.lock` held `72851`.
Without reading any file, the session quoted the last line of `AGENTS.md`, "Keep this file under the 32,768-byte cap that `tests/fm-agents-size.test.sh` enforces.", and the restored section 7 sentence that begins "Complexity alone is not expansion".
It then loaded the `task-lifecycle-reference` skill through the Skill tool and quoted the first sentence under its Validate heading, which matches the pre-move `AGENTS.md` word for word.

In the same clone I ran `codex exec --sandbox read-only` (codex-cli 0.156.0, `gpt-5.6-sol`) and asked it to quote the last sentence of the Maintaining section from its project instructions or reply `TRUNCATED`.
It replied with that same last line of `AGENTS.md`, so Codex received the whole file.
No walk process was left running afterwards.

## Validation

`bin/fm-lint.sh` passed with ShellCheck 0.11.0, and `shellcheck -x` passed on both `AGENTS.md` tests.
`bin/fm-doc-audience-check.sh` printed `ok surfaces=139 local_links=302`.
`bin/fm-test-run.sh --check-coverage` printed `FM_TEST_COVERAGE ok total=192`.
`bin/fm-test-run.sh` passed `tests/fm-agents-size.test.sh`, `tests/fm-agents-coverage.test.sh`, `tests/fm-documentation-audiences.test.sh`, `tests/fm-skills-lock.test.sh`, and `tests/fm-workflow-concurrency.test.sh`.

=== ITEM 1+2: heartbeat drain prints ANCHOR; signal drain does not ===
--- heartbeat drain ---
1787430492	1	heartbeat	-	no changes
ANCHOR (durable truth re-read on this heartbeat; survives context compaction):
standing steers (data/captain.md ## Working style):
  - batch updates
  - no fable for scouts
  - prefer durable notes over chat memory
open decisions: 0 | in-flight tasks: 1
--- odometer after first heartbeat ---
pid=unknown
started_epoch=1787430493
wakes=1
--- last-anchor exists? ---
yes: /tmp/fm-anti-drift-evidence.i4pE8w/heartbeat/state/.last-anchor
--- signal drain (must not contain ANCHOR) ---
1787430494	1	signal	task	task.status
OPEN DECISIONS (still open, folded from the durable status logs - not just the latest line):
task [key=creds] blocked: waiting on credits
OPEN DECISIONS: close one by answering it: bin/fm-send.sh <task> --resolve-key <key> '<answer>'
--- last-anchor after signal? ---
absent (correct)

=== ITEM 2: odometer advice when age threshold exceeded ===
1787430495	1	heartbeat	-	no changes
ANCHOR (durable truth re-read on this heartbeat; survives context compaction):
standing steers (data/captain.md ## Working style):
  - batch updates
  - no fable for scouts
  - prefer durable notes over chat memory
open decisions: 0 | in-flight tasks: 1
odometer: session over threshold (1787430495s elapsed, 1 wakes handled; thresholds 3600s/200) - tell the captain a fresh session is recommended; a restart is a designed non-event.

=== ITEM 1: flags surface ===
1787430496	1	heartbeat	-	no changes
ANCHOR (durable truth re-read on this heartbeat; survives context compaction):
standing steers (data/captain.md ## Working style):
  - batch updates
  - no fable for scouts
  - prefer durable notes over chat memory
open decisions: 0 | in-flight tasks: 1
flags: github-down (clones may be stale; first seen: 2026-08-21T00:00:00Z); watcher-down banner active (guard-key)

=== ITEM 3+5: supervision protocol duty lines ===
--- harness claude (steer + cadence) ---
- Steer capture: before acknowledging any wake, record every new standing captain instruction given this session into data/captain.md (inspect-then-update); durable record, never conversation memory.
- Heartbeat cadence: a heartbeat wake re-reads the ANCHOR block via the drain; refresh the fleet status board (.lavish/fleet-status.html) and batch a captain update whenever anything captain-relevant changed.
--- harness codex (steer + cadence) ---
- Steer capture: before acknowledging any wake, record every new standing captain instruction given this session into data/captain.md (inspect-then-update); durable record, never conversation memory.
- Heartbeat cadence: a heartbeat wake re-reads the ANCHOR block via the drain; refresh the fleet status board (.lavish/fleet-status.html) and batch a captain update whenever anything captain-relevant changed.
--- harness pi (steer + cadence) ---
- Steer capture: before acknowledging any wake, record every new standing captain instruction given this session into data/captain.md (inspect-then-update); durable record, never conversation memory.
- Heartbeat cadence: a heartbeat wake re-reads the ANCHOR block via the drain; refresh the fleet status board (.lavish/fleet-status.html) and batch a captain update whenever anything captain-relevant changed.
--- read-only must omit duties ---
omitted (correct)

=== ITEM 7: generated brief Rules standing rule ===
--- Rules section excerpt ---
# Rules
1. Never push to the default branch. Never merge a PR.
2. Stay inside this worktree; modify nothing outside it.
3. Use gh-axi for GitHub operations and chrome-devtools-axi for browser operations.
4. Report status by appending one line:
   `echo "{state}: {one short line}" >> '/tmp/fm-anti-drift-evidence.i4pE8w/brief-home/state/brief-demo.status'`
   States: working, needs-decision, blocked, paused, done, failed.
   Each append wakes firstmate, so report sparingly: only phase changes a supervisor
   would act on (setup done, bug reproduced, fix implemented, validation passed) and the
   needs-decision/blocked/paused/done/failed states. No step-by-step FYI progress lines;
   firstmate reads your pane for that.
   A mid-task `working:` line (including setup complete) is nonterminal: do not end the
   turn after it; continue the same stage until a defined `done:` gate under Definition of done.
   Use `paused: {why}` - distinct from `blocked:` - ONLY when you are deliberately idling on a
   known external wait you expect to clear on its own (an upstream release, a rate-limit reset,
   a scheduled window): firstmate then leaves your idle pane alone and rechecks it on a long
   cadence instead of treating it as a possible wedge. Use `blocked:` when you are stuck and need help.
5. If you hit the same obstacle twice, append `blocked: {why}` and stop; firstmate will help.
6. If a decision belongs above the implementation worker (product choices, destructive actions, ask-user findings),
   append `needs-decision: {summary of options}` and stop. Firstmate will apply the configured authority and reply with the decision.
   A decision or blocker you opened stays open until a `resolved` line carrying its exact key lands; a later `done:` or `working:` line never closes it, even when the answer is what started that work.
   Firstmate's reply normally writes that closing line at answer time; when a blocker or wait clears WITHOUT a firstmate reply, append `resolved: {how it cleared}` yourself (same `[key=<slug>]` if you opened it with one) as you resume.
7. Never stop, restart, or update the shared `no-mistakes` daemon - it is one instance serving
   every lane/home, so restarting it kills other lanes' in-flight pipeline runs. On ANY no-mistakes
   daemon error, append `blocked: {the daemon error}` and stop; only firstmate manages the daemon.
8. After CI is green and before reporting any PR done, check its review comments and resolve every actionable review-bot finding (including CodeRabbit and Copilot) and human review thread by fixing it or replying with a concrete reason it is not valid.
9. Before reporting done for any PR with user-visible UI changes, use the exact upload command supplied by the project brief to upload viewport screenshots and embed them in the PR body; local paths do not count.
10. Run `npx unslop` on every changed file and fix all findings before any PR.
11. Do not spawn subagents, background agents, or sub-workers; do all work directly in your own session.

--- scout Rules excerpt ---
# Rules
1. Never push to any remote and never open a PR.
2. Stay inside this worktree; the only files you may write outside it are the report and the status file below.
3. Use gh-axi for GitHub operations and chrome-devtools-axi for browser operations.
4. Report status by appending one line:
   `echo "{state}: {one short line}" >> '/tmp/fm-anti-drift-evidence.i4pE8w/brief-home/state/brief-scout.status'`
   States: working, needs-decision, blocked, paused, done, failed.
   Each append wakes firstmate, so report sparingly: only phase changes a supervisor
   would act on and the needs-decision/blocked/paused/done/failed states. No step-by-step
   FYI progress lines; firstmate reads your pane for that.
   Use `paused: {why}` - distinct from `blocked:` - ONLY when you are deliberately idling on a
   known external wait you expect to clear on its own (an upstream release, a rate-limit reset):
   firstmate then leaves your idle pane alone and rechecks it on a long cadence instead of
   treating it as a possible wedge. Use `blocked:` when you are stuck and need help.
5. If you hit the same obstacle twice, append `blocked: {why}` and stop; firstmate will help.
6. If a decision belongs to a human (product choices, destructive actions),
   append `needs-decision: {summary of options}` and stop. Firstmate will reply with the decision.
   A decision or blocker you opened stays open until a `resolved` line carrying its exact key lands; a later `done:` or `working:` line never closes it, even when the answer is what started that work.
   Firstmate's reply normally writes that closing line at answer time; when a blocker or wait clears WITHOUT a firstmate reply, append `resolved: {how it cleared}` yourself (same `[key=<slug>]` if you opened it with one) as you resume.
7. Never stop, restart, or update the shared `no-mistakes` daemon - it is one instance serving
   every lane/home, so restarting it kills other lanes' in-flight pipeline runs. On ANY no-mistakes
   daemon error, append `blocked: {the daemon error}` and stop; only firstmate manages the daemon.
8. Do not spawn subagents, background agents, or sub-workers; do all work directly in your own session.


=== ITEM 4: watcher-beat alarm once per episode ===
--- first stale check ---
notifier log:
osascript	watcher beacon stale 30s (>2x grace 8s) with supervision needed in beat; poll delivery may be stalled. A fresh session or the emitted repair line owns recovery; this alert did not start or stop anything.
marker:
1787430468
--- second same-episode check (must stay silent) ---
notifier log after second run:
osascript	watcher beacon stale 30s (>2x grace 8s) with supervision needed in beat; poll delivery may be stalled. A fresh session or the emitted repair line owns recovery; this alert did not start or stop anything.
--- installer without consent ---
watcher-beat-alarm install:
  label:  com.firstmate.watcher-beat-alarm.74f427b0
  plist:  /tmp/fm-anti-drift-evidence.i4pE8w/launch-agents/com.firstmate.watcher-beat-alarm.74f427b0.plist
  action: write /tmp/fm-anti-drift-evidence.i4pE8w/launch-agents/com.firstmate.watcher-beat-alarm.74f427b0.plist and load it into launchd now (alerts every 120s; alert-only, never starts or stops anything)
Proceed? [y/N] /Users/evanagee/.no-mistakes/worktrees/b99440365b40/01M0JG59ZXVW9PW711KWT0YA5F/bin/fm-watcher-beat-alarm-install.sh: line 80: /dev/tty: Device not configured
refused: consent not given (pass --yes only when the captain already approved this exact action)
plist count: 0

=== ITEM 6: captain repro fold (trailing + bare bracket) ===
trailing-key open:
nm-openai-credits	blocked	the pipeline daemon is out of credits and the user cannot add creditsafter canonical resolve:
bare-bracket open:
died-resume-fold	needs-decision	reviewer flagged TurnDied double-render
=== ITEM 6: mid-note prose vs last-token key (captain option A) ===
--- mid-note: resolved: docs still mention [key=q1] in passing ---
open set:
q1 | needs-decision | real choice--- last-token: resolved: docs still mention [key=q1] ---
open set:
default | needs-decision | should docs mention [key=prose]?